---
title: Field
description: Label, 설명, 에러 메시지를 포함하는 폼 필드 컴포넌트입니다.
---

import { Field } from "@huxley-ui/ui/components/field";

## Preview

<div className="flex flex-col gap-4 p-4 border rounded-lg max-w-sm">
  <Field label="Label" placeholder="Enter text" description="Guide message" />
</div>

## Installation

```tsx title="components/ui/field.tsx"
// Copy from packages/ui/components/field.tsx
```

## Usage

```tsx
import { Field } from "@/components/ui/field";

export function Example() {
  return <Field label="Email" placeholder="you@example.com" description="We'll never share your email." />;
}
```

## Error

```tsx
<Field label="Label" error="Error message appears here" />
```

## Disabled

```tsx
<Field label="Label" disabled description="Guide message" />
```

## Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `label` | `ReactNode` | - | 필드 레이블 |
| `description` | `ReactNode` | - | 가이드 메시지 |
| `error` | `ReactNode` | - | 에러 메시지 |
| `leading` | `ReactNode` | - | 앞쪽 아이콘 |
| `disabled` | `boolean` | `false` | 비활성화 |
